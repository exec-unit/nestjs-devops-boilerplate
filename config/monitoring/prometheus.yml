global:
  scrape_interval: 30s
  evaluation_interval: 1m

scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Future: Add NestJS microservices with @willsoto/nestjs-prometheus endpoints
  # - job_name: 'boilerplate-microservices'
  #   static_configs:
  #     - targets: ['boilerplate-api-gateway:8080', 'boilerplate-user-service:8080']
  #   metrics_path: '/actuator/prometheus'
  #   scrape_interval: 30s
